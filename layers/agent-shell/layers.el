;;; layers.el --- agent-shell layer dependencies  -*- lexical-binding: t; -*-

;; agent-shell has no Spacemacs layer dependencies — shell-maker and acp are
;; declared as MELPA packages in packages.el.

;;; layers.el ends here
