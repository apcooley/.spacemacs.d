;;; config.el --- agent-shell layer config  -*- lexical-binding: t; -*-

;; No layer-level config — the preferred agent config is set in
;; `agent-shell/init-agent-shell' because it depends on
;; `agent-shell-opencode-make-agent-config' being defined first.

;;; config.el ends here
